{
  "name": "doc-beaverx-admin",
  "version": "1.0.0",
  "private": true,
  "description": "BeaverX Admin 前后端保姆级教程文档站",
  "type": "module",
  "scripts": {
    "sync:imgs": "node scripts/sync-imgs.mjs",
    "docs:dev": "npm run sync:imgs && vitepress dev docs",
    "docs:build": "npm run sync:imgs && vitepress build docs",
    "docs:preview": "vitepress preview docs"
  },
  "devDependencies": {
    "vitepress": "^1.6.3"
  }
}
